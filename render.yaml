services:
  - type: web
    name: freshservice-api
    runtime: node
    plan: free
    buildCommand: npm install --omit=dev
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: FRESHSERVICE_COOKIES
        sync: false